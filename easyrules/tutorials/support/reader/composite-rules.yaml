name: Movie id rule
compositeRuleType: UnitRuleGroup
priority: 1
composingRules:
  - name: Time is evening
    description: If it's later than 7pm
    priority: 1
    condition: "20 > 19"
    actions:
      - "print('20 > 19 is right')"
  - name: Movie is rated R
    description: If the movie is rated R
    priority: 1
    condition: "'abc' == 'abc'"
    actions:
      - "print('abc==abc is true')"
---
name: weather rule
description: when it rains, then take an umbrella
priority: 1
condition: "True == True"
actions:
  - "print('True == True is true')"